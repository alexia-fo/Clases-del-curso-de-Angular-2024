// Exportamos dos funciones de este archivo
export function suma(a:number, b:number) {
  return a + b;
}

export function resta(a:number, b:number) {
  return a - b;
}

export default function multiplicacion(a:number, b:number) {
  return a * b;
}
